# AntacinnHelp

แอปพลิเคชันแผนที่เตือนจุดเสี่ยงสำหรับนักท่องเที่ยว พื้นที่อำเภอหาดใหญ่และอำเภอเมืองสงขลา

รายวิชา 344-312 Mobile Application Development ภาคการศึกษาที่ 1/2569

---

## ทำอะไรได้บ้าง

| ช่วงการเดินทาง | ฟีเจอร์ | อ้างอิงเอกสาร |
|---|---|---|
| ก่อนเดินทาง | ค้นหาได้ทั้งสถานที่และจุดเสี่ยง เรียงตามความเกี่ยวข้อง | บทที่ 4 |
| | การ์ดสถานที่ยอดนิยม 18 แห่ง บอกจำนวนจุดเสี่ยงรอบ 2 กม. กด "ดูบนแผนที่" หรือ "นำทางไปที่นี่" | บทที่ 4 |
| | แผนที่จุดเสี่ยง สีหมุดเปลี่ยนตามเดือนและเวลา กรองตามประเภทอันตรายได้ มีคำอธิบายสัญลักษณ์ หมุดข้อมูลทางการขอบสีน้ำเงิน | บทที่ 4, 5.4 |
| | วางแผนเส้นทาง เลือกต้นทาง ("ตำแหน่งของฉัน" หรือสถานที่) และปลายทางเอง เห็นจุดเสี่ยงเรียงตามลำดับที่จะขับผ่าน | ตาราง 3.1 |
| | รายการโปรด กดดาวในหน้ารายละเอียด ขึ้นหน้าแรกทันที | ตาราง 6.1 |
| ระหว่างเดินทาง | เตือนเมื่อเข้าใกล้ 500 ม. ด้วยเสียงพูดภาษาไทย การสั่น และการ์ดที่นับระยะถอยหลังจริง | วัตถุประสงค์ข้อ 4 |
| | บอกจุดเสี่ยงถัดไป "อีก x กม. ตามเส้นทาง" และบอกเมื่อออกนอกเส้นทาง | บทที่ 5.2 |
| | โหมดจำลองการเดินทาง สาธิตได้โดยไม่ต้องขับรถจริง | บทที่ 7.3 |
| | จอไม่ดับเองระหว่างโหมดเดินทาง (จอดับแล้วการเตือนจะหยุด) เครื่องที่สั่งไม่ได้ แอปบอกให้ตั้งเวลาปิดจอเอง | วัตถุประสงค์ข้อ 4 |
| ถึงปลายทาง | รายละเอียดจุด สถิติแยกปี คำแนะนำ แหล่งอ้างอิง ปุ่มโทรฉุกเฉิน | บทที่ 4 |
| | ป้าย "✓ ข้อมูลทางการ" แยกจาก "⚠️ ยังไม่ยืนยัน" (ไม่ใช้สีเขียว เพราะสื่อว่าปลอดภัย) | บทที่ 6.2 |
| | จุดที่ยังไม่มีสถิติขึ้นว่า "ยังไม่มีสถิติ" แทนคะแนน 0 (หลักเดียวกัน: 0 ไม่ได้แปลว่าปลอดภัย) | บทที่ 6.2 |
| | สภาพคลื่น (ชายหาด) และฝน (น้ำตก) ตอนนี้ และแถบเตือนในหน้าแรกเมื่อคลื่นแรง | บทที่ 8 |
| หลังเดินทาง | บันทึกจุดเสี่ยงที่เจอเอง เก็บในเครื่อง | บทที่ 4 |

ใช้งานได้แม้ไม่มีอินเทอร์เน็ต: เว็บติดตั้งลงหน้าจอหลักได้ (PWA) เปิดครั้งแรกแล้ว ครั้งต่อไปเปิดได้แม้ออฟไลน์
ข้อมูลจุดเสี่ยง คำแนะนำ และเส้นทางแนะนำ (มีเส้นทางสำรองในเครื่อง) ใช้ได้ครบ
ส่วนแผนที่พื้นหลัง เส้นทางที่เลือกเอง และสภาพอากาศต้องใช้อินเทอร์เน็ต แอปขึ้นแถบบอกเมื่อออฟไลน์
(เส้นทางที่เลือกเองตอนออฟไลน์แสดงเป็นเส้นตรง พร้อมบอกชัดว่าไม่ใช่ถนนจริง)

---

## ⚠️ อ่านก่อนใช้งาน: สถานะข้อมูล

ข้อมูลจุดเสี่ยงมาจากสองแหล่ง แยกไฟล์กัน รวม **25 จุด** (เป้าในเอกสารบทที่ 1.3 คือ 20–25 จุด)

| ไฟล์ | จำนวน | ที่มา | ป้ายในแอป |
|---|---|---|---|
| `data/riskPoints.json` | 12 จุด (มีเหตุการณ์จริง 4 จุด) | ทีมกรอกจากรายงานข่าว | ⚠️ ยังไม่ยืนยัน |
| `data/officialRiskPoints.json` | 13 จุดบนทางหลวง (หาดใหญ่ 11 เมืองสงขลา 2) | กระทรวงคมนาคม ปี 2563–2568 สร้างด้วย `npm run data:mot` | ✓ ข้อมูลทางการ |

จุดของทีมที่มีเหตุการณ์จริง: ทางขึ้นเขาคอหงส์ · หาดชลาทัศน์ · น้ำตกโตนงาช้าง · สะพานติณสูลานนท์ ช่วงที่ 2
วิธีคัดจุดทางการ และกลุ่มอุบัติเหตุที่ไม่ผ่านเกณฑ์พร้อมเหตุผล อยู่ใน `data/MOT_REPORT.md`

**พิกัดทุกจุดตรวจกับ OpenStreetMap แล้ว** (2026-09-11) ที่มาของแต่ละจุดอยู่ใน `data/SOURCES.md`

### กติกาการตั้ง id

ขึ้นต้นด้วยตัวย่ออำเภอ ตามด้วยชื่อสถานที่และลำดับ คำนำหน้าต้องตรงกับฟิลด์ `district` เสมอ
(`npm test` ตรวจให้อัตโนมัติ)

| คำนำหน้า | อำเภอ |
|---|---|
| `hy-` | หาดใหญ่ |
| `sk-` | เมืองสงขลา |
| `sn-` | สิงหนคร (ใช้กับสะพานติณสูลานนท์ช่วงที่ 2 ซึ่งข้ามไปฝั่งสิงหนคร) |
| `user-` | จุดที่ผู้ใช้บันทึกเอง (สร้างโดยแอป ไม่อยู่ในไฟล์ข้อมูล) |

จุดทางการใช้คำนำหน้าอำเภอเดียวกัน ตามด้วย `mot-` สายทาง และหลักกิโลเมตร เช่น `hy-mot-414-km3-6`

**จุดของทีมอีก 8 จุดยังว่าง** (`incidents: []`, `source: "⚠️ รอกรอกข้อมูลจริง"`) เพราะยังหาแหล่งอ้างอิงระดับจุดไม่ได้
เป็นแบบนี้โดยตั้งใจ ตามกฎที่ทีมเขียนไว้เองในเอกสารบทที่ 6.3:
**ห้ามบันทึกตัวเลขสถิติที่ปราศจากแหล่งอ้างอิง**

### ทำไมจุดของทีมยังเป็น `verified: false`

เพราะ `UNVERIFIED_TEXT` ใน `constants/config.js` ที่ทีมนิยามไว้เองระบุว่า
*"ยังไม่ได้ยืนยันจากเอกสารทางการ อาจมาจากการสอบถามผู้อยู่ในพื้นที่**หรือรายงานข่าว**"*

ข้อมูลที่กรอกไปมาจาก**รายงานข่าว** จึงยังไม่เข้าเกณฑ์ `verified: true` ตามนิยามของทีมเอง
จะเปลี่ยนเป็น `true` ได้ต่อเมื่อมีเอกสารทางการ เช่น บันทึกของ สภ. ในพื้นที่
หรือสถิติจาก ศปถ.จังหวัด ที่ระบุถึงจุดนั้นโดยตรง

### ข้อจำกัดสำคัญของตัวเลขที่กรอกไว้

ตัวเลขมาจากข่าว **ข่าวรายงานเฉพาะเหตุที่รุนแรงหรือมีคนตาย** เหตุเล็กน้อยแทบไม่เป็นข่าว
ดังนั้น **จำนวนจริงย่อมสูงกว่าที่บันทึกไว้เสมอ** และคะแนนที่คำนวณได้คือ *ขั้นต่ำ* ไม่ใช่ค่าจริง
ห้ามตีความว่าจุดที่คะแนนต่ำแปลว่าปลอดภัย

### ข้อมูลทางการที่มี — แต่ใช้รายจุดไม่ได้

ชุดข้อมูล **"สถิติอุบัติเหตุทางถนนในพื้นที่จังหวัดสงขลา"** ของสำนักงานจังหวัดสงขลา
(data.go.th ชุด `dpm08` สัญญาอนุญาต Open Data Common ที่มา: ระบบ e-report)
รายงานช่วง 1 ม.ค. – 13 มิ.ย. 2568 ไว้ว่า

| อำเภอ | อุบัติเหตุ | บาดเจ็บ | เสียชีวิต |
|---|---|---|---|
| หาดใหญ่ | 11 ครั้ง | 3 คน | 9 คน |
| เมืองสงขลา | 20 ครั้ง | 16 คน | 5 คน |

ข้อมูลชุดนี้เป็น **ระดับอำเภอ** ไม่ใช่ระดับจุด จึงเอามาใส่ `incidents` ของจุดใดจุดหนึ่งไม่ได้
การเกลี่ยตัวเลขอำเภอลงไปตามจุดต่าง ๆ ก็เท่ากับแต่งข้อมูลขึ้นเอง จึงไม่ทำ
ใช้เป็นบริบทภาพรวมได้เท่านั้น

### วิธีกรอกข้อมูลเพิ่ม

เติม `incidents`, `peakMonths`, `peakHours`, `advice`, `source` ในไฟล์ `data/riskPoints.json`
คะแนนกับสีหมุดจะเปลี่ยนตามเอง **ไม่ต้องแก้โค้ดสักบรรทัด**

- ใส่ `peakMonths` / `peakHours` **เฉพาะเมื่อมีหลักฐานรองรับ** เพราะสองค่านี้เป็นตัวคูณคะแนน
  (ฤดูกาล 1.3 เท่า เวลา 1.1 เท่า) ใส่มั่วจะดันคะแนนให้สูงเกินจริง
- ทุกรายการใน `incidents` ควรมี `note` บอกว่าเหตุการณ์นั้นคืออะไรและเกิดวันไหน
- `source` ต้องระบุชื่อสำนักข่าวหรือหน่วยงาน พร้อมวันที่ ให้คนอื่นตามไปตรวจสอบได้
- จุดบนทางหลวงไม่ต้องกรอกเอง ปีหน้ากระทรวงคมนาคมเปิดข้อมูลปีใหม่แล้ว รัน `npm run data:mot`
  ไฟล์จุดทางการและรายงานจะอัปเดตเอง (ต้องต่ออินเทอร์เน็ต ใช้เวลาราว 1 นาที)

---

## วิธีรัน

| ช่องทาง | ลิงก์ |
|---|---|
| เว็บจริง (ใครก็เปิดได้ อัปเดตเองทุกครั้งที่ push) | https://remote55.github.io/antacinn-help/ |
| Expo Snack (ส่งงานรายวิชา) | https://snack.expo.dev/KXKjp99In3BgS5P3M1Ba6 |
| โค้ดทั้งหมด | https://github.com/Remote55/antacinn-help |

### บนมือถือ (แนะนำ — แผนที่สมบูรณ์ที่สุด)
1. ติดตั้งแอป **Expo Go** จาก Play Store / App Store
2. เปิดลิงก์ Snack ข้างบน กด **Run on device** แล้วสแกน QR code

### ติดตั้งเป็นแอปจากเว็บจริง (ไม่ต้องมี Expo Go)
- Android (Chrome): เปิดเว็บจริง → เมนู ⋮ → **ติดตั้งแอป** หรือ **เพิ่มลงในหน้าจอหลัก**
- iPhone (Safari): เปิดเว็บจริง → ปุ่มแชร์ → **เพิ่มไปยังหน้าจอโฮม**

### บนเว็บผ่าน Snack
กดแท็บ **Web** ใน Snack ได้เลย — แผนที่จะใช้ Leaflet แทน react-native-maps โดยอัตโนมัติ

### อัปโหลดโค้ดชุดใหม่ขึ้น Snack
```bash
npm run snack:upload
```
พิมพ์ลิงก์ Snack ใหม่ออกมา (ทุกครั้งได้ลิงก์ใหม่ เอาไปแทนลิงก์ในตารางข้างบน) ใช้ `-- --check` เพื่อตรวจอย่างเดียวไม่บันทึก
หมายเหตุ: `@react-navigation/native-stack` ล็อกไว้ที่ 7.18.8 เพราะ Snack build เวอร์ชัน 7.18.9 ขึ้นไปไม่ได้ (ตรวจเมื่อ 2026-09-11) ห้ามอัปเดตก่อนตรวจด้วย `--check`

### รันบนเครื่องตัวเอง
```bash
npm install
npx expo start
```
กด `w` เพื่อเปิดบนเว็บ หรือสแกน QR code ด้วย Expo Go

### build เว็บแบบเดียวกับเว็บจริง
```bash
npm run build:web
```
ได้โฟลเดอร์ `dist` พร้อม `sw.js` (ตัวที่ทำให้เปิดเว็บได้ตอนออฟไลน์) ต้องเปิดผ่านเว็บเซิร์ฟเวอร์ เช่น `python -m http.server --directory dist`
ตอนพัฒนาด้วย `npx expo start` จะไม่มีโหมดออฟไลน์ ตั้งใจให้เป็นแบบนั้น จะได้ไม่เจอโค้ดเก่าค้างในเครื่อง
ไอคอนเว็บแอปสร้างด้วย `npm run icons` (รันใหม่เฉพาะตอนเปลี่ยนรูปไอคอน)

### รันเทสต์คณิตศาสตร์ (ไม่ต้องใช้ Snack ไม่ต้องต่อเน็ต)
```bash
npm test
```
ต้องได้ `pass 256` `fail 0` (รวมการตรวจความถูกต้องของไฟล์ข้อมูล และความคมชัดของทุกคู่สี)

### ตรวจก่อนอัปโหลดขึ้น Snack ทุกครั้ง
```bash
npm run check:imports
```
ต้องได้ `OK: ทุก import หาไฟล์เจอ และทุกชื่อมีอยู่จริง`
(Metro build ผ่านได้แม้ import ชื่อที่ไม่มีอยู่จริง แล้วไปพังตอนผู้ใช้กดปุ่มนั้น)

### สาธิตโหมดเดินทางโดยไม่ต้องขับรถ
1. แท็บ **เส้นทาง** → เลือกเส้นทาง → กด **▶ จำลองการเดินทาง**
2. จุดสีฟ้าจะวิ่งตามถนนที่ 60 กม./ชม. เร่งเวลา ×10 (เลือก ×30 ถ้าอยากให้จบเร็ว)
3. แอปพูดบอกล่วงหน้า *"จุดเสี่ยงถัดไป ... อีก 1 กิโลเมตร ข้างหน้า"*
   และเตือนเมื่อเข้าใกล้ 500 ม. *"ระวัง! อีกประมาณ 400 เมตร ... ลดความเร็ว"* พร้อมสั่น
4. เครื่องที่ไม่มีเสียงภาษาไทย แอปจะบอกบนจอและเตือนด้วยภาพกับการสั่นแทน กด **ทดสอบเสียง** เพื่อเช็กก่อน

ของจริงใช้ปุ่ม **📍 เริ่มจริงด้วย GPS** ตรรกะการเตือนเป็นชุดเดียวกัน ต่างกันแค่แหล่งตำแหน่ง

---

## โครงสร้าง

| โฟลเดอร์ | หน้าที่ | ทดสอบ |
|---|---|---|
| `utils/` | คณิตศาสตร์ล้วน **ห้าม import react เด็ดขาด** | 250 เทสต์ |
| `data/` | จุดเสี่ยงของทีมและจุดทางการ สถานที่ยอดนิยม เส้นทางสำรองออฟไลน์ และที่มาของข้อมูล (`SOURCES.md`, `MOT_REPORT.md`) | 6 เทสต์ ตรวจทุกไฟล์ตามกติกา |
| `scripts/` | `check-imports.mjs` ตรวจ import ก่อนอัปโหลด, `mot-accidents.mjs` สร้างจุดทางการ, `upload-snack.mjs` อัปโหลดขึ้น Snack, `build-sw.mjs` + `sw-template.js` สร้าง service worker หลัง build เว็บ, `make-icons.mjs` วาดไอคอน | รันด้วย npm run |
| `public/` | แม่แบบหน้าเว็บ manifest และไอคอนของเว็บแอป (Expo คัดลอกไปไว้ใน `dist` ตอน build) | ตรวจใน Chrome |
| `.github/workflows/` | build และขึ้นเว็บ GitHub Pages อัตโนมัติ (รันเทสต์ก่อน ไม่ผ่านไม่ขึ้นเว็บ) | ทุกครั้งที่ push |
| `hooks/` | GPS, การเก็บข้อมูลลงเครื่อง, state | ทดสอบตอนรันแอป |
| `components/` | UI ที่ใช้ซ้ำหลายหน้า | ทดสอบตอนรันแอป |
| `screens/` | 6 หน้าจอของแอป | ทดสอบตอนรันแอป |
| `navigation/` | ผูกหน้าจอเข้าด้วยกัน | ทดสอบตอนรันแอป |

### กติกาที่ต้องรู้ก่อนแก้โค้ด

**1. นามสกุล `.js` ในบรรทัด import ไม่เหมือนกันแต่ละโฟลเดอร์ — ตั้งใจให้เป็นแบบนั้น**
- `utils/` กับ `tests/` **ต้องใส่** `.js` เพราะ Node รันตรง ๆ และ Node ไม่เติมนามสกุลให้
- `hooks/`, `components/`, `screens/`, `navigation/` **ห้ามใส่** เพราะ Metro เติมให้เอง

**2. แผนที่มีสองไฟล์ ห้ามแก้ไฟล์เดียว**
`AppMap.native.js` (มือถือ) กับ `AppMap.web.js` (เว็บ) ต้องรับ props ชุดเดียวกันเสมอ
Metro เลือกไฟล์ให้อัตโนมัติ หน้าจอเขียนแค่ `import AppMap from '../components/AppMap'`
`react-native-maps` ใช้บนเว็บไม่ได้ จึงต้องอยู่ใน `.native.js` เท่านั้น
ส่วน `AppMap.js` ไม่ถูกใช้ตอนรันจริง มีไว้ให้ตัวตรวจโค้ดของ Snack หาไฟล์เจอ (ห้ามลบ ไม่งั้น Snack ขึ้นแถบแดง)

**3. ห้ามมีสีเขียวใน `RISK_LEVELS`**
การไม่มีหมุดแปลว่า "ยังไม่มีข้อมูล" ไม่ได้แปลว่า "ปลอดภัย" — มีเทสต์คุมไว้แล้ว

**4. ค่าคงที่ในการคิดคะแนนห้ามแก้มั่ว**
`N_MAX=30`, `SEASON_PEAK=1.3`, `TIME_PEAK=1.1` ถอดมาจากม็อกอัพของทีม
มีเทสต์ชื่อ "ตรงกับม็อกอัพ" คุมไว้ว่าเขาคอหงส์ต้องได้ 43 พอดี ถ้าแก้แล้วเทสต์จะแดง

**5. เปลี่ยนสีแล้วต้องอ่านออก**
ตัวอักษรบนป้ายระดับความเสี่ยงใช้ `textColor` ของระดับนั้น (เข้มบนเหลืองและส้ม ขาวบนแดง)
`tests/color.test.js` ตรวจทุกคู่สีตัวอักษร/พื้นที่แอปใช้ ตามเกณฑ์ WCAG 4.5:1 เปลี่ยนสีแล้วอ่านไม่ออก เทสต์จะแดง

---

## สถานะการตรวจสอบ (2026-09-12)

ตรวจในเบราว์เซอร์จริงที่ขนาดจอมือถือ 375×812

| สิ่งที่ตรวจ | ผล |
|---|---|
| เทสต์ทั้งหมด | 256/256 ผ่าน (รวมตรวจไฟล์ข้อมูลและความคมชัดของสี) |
| `npm run check:imports` (211 บรรทัด import) | ทุกไฟล์หาเจอ ทุกชื่อมีอยู่จริง |
| build มือถือ `npx expo export --platform android` | สำเร็จ |
| build เว็บด้วย Metro | สำเร็จ ไม่มี react-native-maps หลุดเข้าบันเดิลเว็บ |
| แผนที่ Leaflet บนเว็บ | หมุดครบ 25 จุด (ทีม 12 ทางการ 13) ตรงพิกัด |
| ค้นหา "หาด" | ชายหาดสองแห่งขึ้นก่อน |
| เส้นทาง หาดใหญ่ → โตนงาช้าง | 29.2 กม. แสดงตัวน้ำตกเป็นจุดสุดท้าย |
| เข้าแล้วออกโหมดเดินทาง | ไม่จอขาว ไม่มี error ใน console |
| บันทึกจุดใหม่ | โผล่ในแท็บแผนที่ทันที และยังอยู่หลังโหลดหน้าใหม่ |
| จำลองการเดินทาง ม.อ. → หาดสมิหลา (×30) | พูดบอกล่วงหน้าและพูดเตือนครบทั้ง 7 จุดบนเส้นทาง (จุดทางการบนถนนกาญจนวนิชและลพบุรีราเมศวร์ 4 จุด) เรียงตามลำดับที่ขับผ่าน |
| การ์ดเตือนระหว่างขับ | ระยะนับถอยหลังจริง แล้วปิดเองเมื่อผ่านจุดไปเกิน 800 ม. |
| ถึงปลายทาง | แถบขึ้น "ผ่านจุดเสี่ยงบนเส้นทางครบแล้ว" |
| กด 🔇 | ไม่มีเสียงพูด แต่การ์ดเตือนยังขึ้นตามปกติ |
| โหมด GPS ห่างเส้นทาง 1.1 กม. | แถบขึ้น "ออกนอกเส้นทางที่วางแผนไว้" แล้วเตือนด้วยระยะเส้นตรงแทน |
| การ์ดสถานที่ยอดนิยม 18 แห่ง | จำนวนจุดเสี่ยงรอบ 2 กม. ตรงกับที่คำนวณจากไฟล์ข้อมูล ที่ที่ไม่มีข้อมูลบอกว่า "ยังไม่มีข้อมูล" |
| นำทางไปหาดชลาทัศน์จากตำแหน่งปัจจุบัน | ได้เส้นทางจริง 27.9 กม. เห็นจุดเสี่ยง 3 จุด แผนที่ซูมเห็นทั้งเส้น |
| GPS ถูกปฏิเสธ / อยู่นอกพื้นที่ | บอกเหตุผล และคงต้นทางเดิมไว้ |
| ไม่มีอินเทอร์เน็ต | เส้นทางแนะนำใช้เส้นทางสำรอง เส้นทางที่เลือกเองบอกชัดว่าเป็นเส้นตรง ไม่ใช่ถนนจริง |
| รายการโปรด | กดดาวแล้วขึ้นหน้าแรกทันที ยังอยู่หลังโหลดหน้าใหม่ ลบจุดที่บันทึกเองแล้วหลุดจากรายการโปรดด้วย |
| ลบจุดที่บันทึกเองบนเว็บ | ลบได้ (เดิมลบไม่ได้ เพราะ `Alert.alert` ของ react-native-web ไม่ทำอะไรเลย) |
| โหมดเดินทาง GPS ที่ถูกปฏิเสธสิทธิ์ | มีปุ่ม "ใช้โหมดจำลองการเดินทางแทน" กดแล้ววิ่งต่อได้ทันที |
| `npm run data:mot` | ดึงข้อมูลกระทรวงคมนาคม 6 ปี 1,466 เหตุการณ์ ได้จุดทางการ 13 จุด พร้อมรายงาน |
| หน้ารายละเอียดจุดทางการ | ป้าย "✓ ข้อมูลทางการ" กล่องสีฟ้า สถิติรายปี ตำรวจทางหลวง 1193 และแหล่งอ้างอิงที่ตรวจย้อนได้ |
| หน้ารายละเอียดหาดสมิหลา | คลื่น 0.3 ม. "ทะเลสงบ" ตรงกับที่ Open-Meteo ตอบ (0.26 ม.) พร้อมบอกว่าค่ามาจากทะเลนอกชายฝั่ง 15 กม. |
| หน้ารายละเอียดน้ำตกโตนงาช้าง | ฝน 0.8 มม./ชม. "ฝนเล็กน้อย" |
| ตัดการเชื่อมต่อ Open-Meteo | ไม่มีการ์ดสภาพตอนนี้ ไม่แสดงค่าเก่า ไม่มี error |
| คลื่นปลอม 1.6 ม. | หน้าแรกขึ้นแถบเตือน "คลื่นแรง · ไม่ควรลงเล่นน้ำ" |
| เข้าออกโหมดเดินทาง 3 รอบ สลับเส้นทาง เปิดดูสถานที่บนแผนที่ | ไม่มี error ใน console |
| จอไม่ดับในโหมดเดินทาง (เว็บ) | ขอล็อกจอเมื่อเข้า ขอใหม่หลังสลับแอปกลับมา ปล่อยเมื่อออก เบราว์เซอร์ปฏิเสธ → ขึ้นข้อความแนะนำ |
| ป้ายระดับความเสี่ยง | ตัวอักษรเข้มบนเหลือง (10.5:1) และส้ม (6.4:1) ขาวบนแดง (5.0:1) · จุดไม่มีสถิติขึ้น "ยังไม่มีสถิติ" |
| ข้อมูลในเครื่องเสีย (ไม่ใช่อาเรย์ / ไม่มีพิกัด / ถูกแก้เป็น verified) | แอปเปิดได้ ทิ้งรายการเสีย จุดที่ถูกแก้ยังขึ้น "⚠️ ยังไม่ยืนยัน" |
| หน้าจอพังระหว่างวาด (จำลอง) | ขึ้นหน้าขอโทษภาษาไทย ปุ่มลองใหม่พากลับหน้าแรก และปุ่มโทร 1669 |
| แผนที่ | หมุดทางการขอบสีน้ำเงิน 13 จุด คำอธิบายสัญลักษณ์ไม่บังปุ่ม "ตำแหน่งของฉัน" |
| ปิดเว็บเซิร์ฟเวอร์แล้วเปิดเว็บใน Chrome | เปิดได้ครบจาก service worker แผนที่ขึ้นหมุด 25 จุด แถบบอกว่าออฟไลน์ |
| build ใหม่ / build ซ้ำโค้ดเดิม | เวอร์ชันใหม่ติดตั้งแทนและลบของเก่า / เวอร์ชันเท่าเดิม ผู้ใช้ไม่ต้องโหลดใหม่ |
